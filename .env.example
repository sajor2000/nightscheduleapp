# 🌍 Environment Variables for Vercel + Supabase
# Copy these exact variables to Vercel dashboard → Settings → Environment Variables

# 🗄️ Database Connection (REQUIRED)
# Use your POSTGRES_URL from Supabase
DATABASE_URL=postgres://postgres.your-project-ref:your-password@aws-0-us-east-1.pooler.supabase.com:6543/postgres?sslmode=require

# ⚙️ Flask Configuration (REQUIRED)
FLASK_ENV=production
FLASK_DEBUG=false

# 🐍 Python Runtime (REQUIRED) 
PYTHONPATH=/var/task

# 📝 Frontend Environment (REQUIRED)
REACT_APP_ENVIRONMENT=production

# 💡 Optional Supabase Variables (for future features)
# SUPABASE_URL=https://your-project-ref.supabase.co
# SUPABASE_ANON_KEY=your-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# 📋 Instructions:
# 1. Replace "your-project-ref" and "your-password" with your actual Supabase credentials
# 2. Add these 5 required variables to Vercel
# 3. Deploy!

# ⚠️ SECURITY: Never commit your actual .env file to Git!